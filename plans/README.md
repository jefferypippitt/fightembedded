# Implementation Plans

Generated by the improve skill on 2026-07-09 (quick audit at `8dda264`). Reviewed/tightened 2026-07-10. Executed 2026-07-10 on branch `advisor/001-004-batch`. Batch 2 plans 005-010 executed 2026-07-10 on branch `advisor/005-010-batch`.

Batch 2 (plans 005-036) generated 2026-07-10 (standard-depth audit at `4633e33`), covering all 9 audit categories. All 26 non-direction findings plus 2 selected direction spikes were planned per explicit maintainer request ("do all of them").

**Constraint from maintainer:** do not install Upstash (or any new Redis/rate-limit SaaS). Keep the existing in-memory `lib/rate-limit.ts` as-is for this batch.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Preserve Unauthorized through catch blocks | P1 | S | — | DONE |
| 002  | Validate and rate-limit updateAthleteRanks | P1 | S | 001 | DONE |
| 003  | Reject impossible fight-record combinations | P2 | S | — | DONE |
| 004  | Add characterization tests for mutation contracts | P2 | M | 001, 002, 003 | DONE |
| 005  | Fix betterAuth cors→trustedOrigins misconfiguration | P1 | S | — | DONE |
| 006  | Harden rate-limit identifier against IP spoofing | P2 | S | — | DONE |
| 007  | Fix event validation error handling + form-data helper | P1 | S/M | — | DONE |
| 008  | Fix String(undefined) coercion in athlete form-data | P2 | S | — | DONE |
| 009  | Remove dead athlete query/mutation functions | P2 | S | — | DONE |
| 010  | Add existence check to deleteEvent | P3 | S | — | DONE |
| 011  | Trim unbounded public athlete queries (select) | P2 | S | 009 | TODO |
| 012  | DB-level pagination for admin athlete table | P2 | M | 016 | TODO |
| 013  | Add missing Prisma indexes | P3 | S | — | TODO |
| 014  | DB-side division-stats aggregation | P3 | M | 018 | TODO |
| 015  | Dynamic-import recharts chart components | P3 | S | — | TODO |
| 016  | Characterize + extract paginated-athletes merge logic | P1 | M | — | TODO |
| 017  | Unit tests for quick-stats/utils pure helpers | P2 | S | — | TODO |
| 018  | Characterize + extract dashboard/division stats aggregation | P2 | M | — | TODO |
| 019  | Extract + test client-side rank-recompute logic | P1 | M | — | TODO |
| 020  | Extract + test athlete cache-revalidation logic | P2 | L | 009 | TODO |
| 021  | Extract shared checkAuth() helper | P2 | S | — | TODO |
| 022  | Consolidate division-slug mapping | P2 | S/M | 009 | TODO |
| 023  | Consolidate get-paginated-athletes orderBy branches | P3 | M | 016 | TODO |
| 024  | Split athlete.ts into queries/mutations files | P3 | L | 009, 020, 022 | TODO |
| 025  | Consolidate sort comparators + view-change effects | P3 | M | 019 | TODO |
| 026  | Remove unused oslo dependency | P3 | S | — | TODO |
| 027  | Remove redundant individual @radix-ui/react-* packages | P3 | S | — | TODO |
| 028  | Spike: Prisma 5→6 upgrade | P3 | L | — | TODO |
| 029  | Bump eslint-config-next to match Next version | P3 | S | — | TODO |
| 030  | Add CI workflow (typecheck/lint/test) | P2 | S | — | TODO |
| 031  | Add pre-commit hooks (husky + lint-staged) | P3 | S | — | TODO |
| 032  | Rewrite README with real setup instructions | P3 | S | 033 | TODO |
| 033  | Add .env.example | P2 | S | — | TODO |
| 034  | Add CLAUDE.md + schema doc comments | P2 | S/M | — | TODO |
| 035  | Spike: Fight/Bout relational model design | P4 | L | — | TODO |
| 036  | Spike: automate EventStatus transitions | P4 | S-M | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Execution record (2026-07-10)

- **Branch:** `advisor/001-004-batch`
- **Worktree:** `C:\Users\Jeffery\.cursor\worktrees\advisor-batch-a9132bc6`
- **Executor:** [batch 001–004](3b6dcb23-8740-4f03-9301-6a0278dbf418)
- **Commits (oldest→newest):**
  1. `4075932` Add vitest verification baseline (Step 0 — HEAD lacked vitest)
  2. `f9482a4` Preserve Unauthorized errors in athlete/event actions
  3. `f4d802e` Reject fight records where finishes exceed wins
  4. `e055103` Validate and rate-limit athlete rank updates
  5. `39e4cbc` Add characterization tests for athlete mutation contracts
- **Reviewer verification:** `pnpm typecheck` exit 0; `pnpm test` 23/23 pass; scope clean; FormData extract matches live `imageUrl: String(...)`; Unauthorized rethrow/return present on all listed actions.
- **Not merged** into the user's working branch — merge/checkout is the maintainer's decision.

## Execution record (2026-07-10, batch 005-010)

- **Branch:** `advisor/005-010-batch`
- **Worktree:** `C:\Users\Jeffery\fightembedded`
- **Commits (oldest→newest):**
  1. `a15be26` Fix dead cors option in better-auth config
  2. `5ba8435` Key rate limits off session user ID for authenticated mutations
  3. `374f11c` Surface Zod validation errors in event mutations
  4. `6901afb` Prevent String(undefined) from masking missing athlete fields
  5. `29f5ba1` Remove unused duplicate athlete query functions
  6. `c213fb7` Add not-found check to deleteEvent
- **Reviewer verification:** `pnpm typecheck` exit 0; `pnpm test` 35/35 pass; `pnpm build` exit 0.
- **Deviations:** Plan 007 event-form-data test uses a too-short `venue` value instead of a deleted key — `String(undefined)` on omitted fields still produces `"undefined"` (same latent issue as athlete fields before plan 008); flagged for a follow-up if desired.
- **Not merged** into the user's working branch — merge/checkout is the maintainer's decision.

## Dependency notes

- **002 after 001**: rank-update error handling should use the same Unauthorized preservation pattern introduced in 001.
- **004 last**: tests should lock in the auth, rank-validation, and fight-record rules from 001–003; also extracts `isUnauthorizedError` shared in 001.
- **003 is independent of 001/002** and may run in parallel with 001 if desired.

## Review notes (2026-07-10)

Plans re-checked against live code at `8dda264` (+ uncommitted WIP). Tightened before execute:

- **002**: Current state now distinguishes HEAD (`Promise.all`) vs possible uncommitted `$transaction`; executor must not revert transaction work.
- **004**: FormData extract must match live parsing exactly (`imageUrl: String(rawData.imageUrl)`); `events.ts` added to scope/drift check.
- **001**: Clarified local helper is temporary until 004 extracts it.

## Findings considered and rejected

- **Distributed rate limiting (Upstash/Redis)**: maintainer declined new infra; leave in-memory limiter documented as best-effort on serverless.
- **proxy.ts unused**: false positive — Next.js 16 `proxy.ts` convention; dashboard layout also checks session.
- **Public read actions without auth**: intentional for the public site.
- **Transitive `pnpm audit` highs** (recharts→lodash, better-auth→defu): deferred; not in this batch.

## Batch 2 dependency notes (plans 005-036)

- **016 before 012 and 023**: characterization tests for the current merge/paginate behavior must exist before either the DB-level pagination rewrite (012) or the orderBy consolidation (023) touches that logic.
- **018 before 014**: characterization tests for the current date/win-rate aggregation must exist before the DB-side rewrite (014).
- **009 before 011, 020, 022, 024**: dead-code removal should land first so later plans (query trimming, revalidation extraction, division-slug consolidation, the file split) aren't edited against soon-to-be-deleted code.
- **020, 022 before 024**: the `athlete.ts` file split should happen after the revalidation-logic and division-slug extractions shrink and clean the file, keeping each step's diff reviewable in isolation.
- **019 before 025**: both touch `athletes-data-table.tsx`; land 019's rank-recompute extraction first to avoid overlapping diffs (025 touches non-overlapping regions of the same file).
- **033 before 032** (loose): the README rewrite references `.env.example` by name; either order works since 032's own env-var enumeration is self-sufficient, but landing 033 first avoids a dangling reference.
- **021 (checkAuth extraction) is a soft prerequisite for 024**: not a hard dependency, but the file-split plan checks whether 021 has landed and adjusts its checkAuth-import guidance accordingly.
- Everything else in 005-036 is independent and can be executed in any order, subject to normal diff-conflict hygiene (several plans touch `server/actions/athlete.ts`, `events.ts`, or `get-paginated-athletes.ts` — land those sequentially, not in parallel, even where no formal dependency is declared).

## Batch 2 scope decisions (not rejections — recorded so they aren't re-litigated)

- **CORRECTNESS-01 and DEBT-04 merged into plan 007**: both findings were "events.ts doesn't handle validation errors like athlete.ts does" — one fix covers both.
- **TEST-05 and DEBT-07 merged into plan 020**: the cache-tag/path revalidation test-coverage gap and the revalidation-list duplication are the same code region; one extraction serves both.
- **CORRECTNESS-04 (updateAthleteStatus missing rate limit) folded into plan 009**: since the function is dead code (zero callers, verified), deletion supersedes adding rate-limiting to it.
- **DOCS-02 (no CONTRIBUTING.md) folded into plan 034**: not worth a separate artifact per the original audit's own note; content goes in CLAUDE.md instead.
- **DEP-04 (zod/@hookform/resolvers version coupling) folded into plan 034**: it's a standing tripwire note, not an active bug — documented in CLAUDE.md rather than given its own plan.
- **DIR-03 (shareable athlete-comparison page) and DIR-04 (public division-growth charts) were not planned**: only DIR-01 (Fight/Bout model) and DIR-02 (EventStatus automation) were presented to the maintainer and selected; DIR-03/04 remain available as direction options for a future `/improve next` pass if of interest.
- **Direction findings (035, 036) are spikes, not build plans**: per the skill's own guidance, selected direction findings become design/spike plans, not full implementations — both explicitly stop short of merging schema/infra changes.
